version: '3.8'

services:
  # Backend API Service
  api:
    build:
      context: .
      target: runtime
    container_name: doctalk-api
    command: uvicorn src.api.chat:app --host 0.0.0.0 --port 8000
    ports:
      - "9090:8000"
    env_file:
      - .env.local
    environment:
      - LOG_LEVEL=INFO
      - OLLAMA_BASE_URL=http://host.docker.internal:11434
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - doctalk-net
    restart: unless-stopped

  # Frontend UI Service
  ui:
    build:
      context: .
      target: runtime
    container_name: doctalk-ui
    command: python -m streamlit run src/ui/chat.py --server.port 8501 --server.address 0.0.0.0 --server.maxUploadSize 2
    ports:
      - "9091:8501"
    env_file:
      - .env.local
    environment:
      # Docker networking: "api" resolves to the backend container IP
      - APP_API_URL=http://api:8000
    depends_on:
      - api
    networks:
      - doctalk-net
    restart: unless-stopped

networks:
  doctalk-net:
    driver: bridge
